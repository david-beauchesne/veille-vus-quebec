[project]
name = "veille-vus"
version = "0.1.0"
description = "Veille locale d'annonces de VUS usages au Quebec"
requires-python = ">=3.11"

[project.scripts]
veille-vus = "veille_vus.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

